/**
 * Define the vertex which contains
 * x, y in 2D space, transform
 * the point to the float point
 */
class Vertex2D {
    constructor(x, y) {
        this.x = parseFloat(x);
        this.y = parseFloat(y);
    }
}